import 'package:flutter/material.dart';

class OverlayOn extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return new Container(
      width: 300,
      height: 300,
      color: Colors.black,
      child: new Container(
        //onTap: () => print('hola'),
        /*child: new Column(
          mainAxisAlignment: MainAxisAlignment.center,
          children: <Widget>[
            new Container(
              
            ),
            new Padding(
              padding: new EdgeInsets.only(bottom: 20.0),
            ),
            
          ],
        ),*/
      ),
    );
  }
}